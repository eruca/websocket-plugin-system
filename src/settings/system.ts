export const websocketAddr = `ws://${document.location.hostname}:9000/ws`;
// export const websocketAddr = 'ws://echo.websocket.org';
export const httpAddr = `http://${document.location.hostname}:9000`;

export const skips = [];
